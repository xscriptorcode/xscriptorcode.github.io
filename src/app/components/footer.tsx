import { PagesStyles } from "./pagesstyles";

export default function Footer() {
  return (
    <footer className={`${PagesStyles.footerStyles}`}>
      <h3>Xscriptor/Code &copy;</h3>
    </footer>
  );
}
